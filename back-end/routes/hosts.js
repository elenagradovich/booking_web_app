const {Router} = require('express')
const hostsController = require('../controllers/hosts-controller')
const router = Router()

//router.get('/', hostsController.getHosts)
//router.get('/', hostsController.getCities)

module.exports = router